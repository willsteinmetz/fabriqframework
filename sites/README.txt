To create a new site:
 - Create a copy of the skeleton directory
 - Rename the copy directory to the URL of the new site removing the http:// or https://
   - ex: http://example.com/site would have the directory example.com
   - Multi-site currently only supports 
 - Be sure that your domain points to this copy of the code

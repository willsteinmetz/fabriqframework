To create a new site:
 - Create a copy of the skeleton directory
 - Rename the copy directory to the URL of the new site replacing slashes (/) with underscores (_)
   and removing the http:// or https://
   - ex: http://example.com/site would have the directory example.com_site
